import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static output for maximum performance
  // Remove if you need server-side rendering with dynamic data
};

export default nextConfig;
